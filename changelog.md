### Added
- **Slot Locking System** — Each slot can now be individually locked to restrict item input
    - Press **Save** to snapshot the current items in each slot as the allowed items
    - Press **Lock** to enforce the saved configuration — only saved items can be inserted when locked
    - When **unlocked**, any valid item can be inserted freely as before